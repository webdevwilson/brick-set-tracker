{
  "name": "brick-sheet-api",
  "version": "1.0.0",
  "description": "Cloud Function API for fetching LEGO set data from Firestore",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "deploy": "gcloud functions deploy fetchLegoSet --runtime nodejs20 --trigger-http --allow-unauthenticated --region us-central1"
  },
  "dependencies": {
    "@google-cloud/firestore": "^7.1.0",
    "@google-cloud/functions-framework": "^3.3.0",
    "google-auth-library": "^9.6.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
